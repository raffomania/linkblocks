// THIS FILE HAS NO EFFECT ON CSS GENERATION
// We only include this file to enable the VSCode Tailwind extension.
// Without it, the extension will disable itself.
// To configure how tailwind CSS gets generated, look at the `build.rs` file.
module.exports = {
  content: ["src/views/*.rs"],
};
